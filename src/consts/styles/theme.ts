const light = {
    textColor: "black",
    backgroundColor: "white",
}

const dark = {
    textColor: "white",
    backgroundColor: "black",
}

const theme = {
    light,
    dark,
}

export default theme;